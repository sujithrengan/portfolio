/** @type {import('next').NextConfig} */
module.exports = {
  // output: 'export',
  reactStrictMode: true,
  experimental: {
    outputStandalone: true,
  },
  // images: {
  //   unoptimized: true,
  // },
}